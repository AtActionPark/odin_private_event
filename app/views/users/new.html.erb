<div class="center jumbotron">
  <h1>Sign in </h1>
  <h6> *foo, bar, or foobar are users*</h6>

  <%= form_for :user, url: users_path do |f| %>

  <%= f.label :name %>
  <%= f.text_field :name %>


  <%= f.submit "Submit", class: "btn btn-lg btn-primary" %>

  <% end %>

</div>