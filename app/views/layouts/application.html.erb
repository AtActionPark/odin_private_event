<!DOCTYPE html>
<html>
<head>
  <title>PrivateEvent</title>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>
<body>
<header class="navbar navbar-fixed-top navbar-inverse">
    <div class:"container">
      <%= link_to "Private Events", events_path, id: "logo" %>

      <ul class="nav navbar-nav navbar-right">
        <li><%= link_to "Events", events_path %></li>
      
        <% if signed_in?%>
        <li><%= link_to "Create event", create_event_path %></li>
        <li><%= link_to "Profile", current_user %></li>
          <li><%= link_to "Logout #{current_user.name}", signout_path , method: "delete"%></li>
        <% else %>
          <li><%= link_to "Login", signin_path %></li>
        <% end %>
      </ul>
    </div>
  </header>
<div class="container">
  <% flash.each do |message_type, message| %>
    <div class="alert alert-<%= message_type %>"><%= message %></div>
  <% end %>
  <%= yield %>
  <%= debug(params) if Rails.env.development? %>
</div>
 <footer class="footer">
     <small>
      Exercice for  <a href="http://www.theodinproject.com/ruby-on-rails/associations">The Odin Project</a>.
      See code on <a href="https://github.com/AtActionPark/odin_private_event">Github</a>
    </small>
  </footer>
</body>
</html>
